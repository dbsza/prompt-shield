{
  "name": "@prompt-shield/detection-engine",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg",
    "test": "cargo test",
    "clean": "cargo clean && rm -rf pkg"
  }
}
